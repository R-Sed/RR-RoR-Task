<% content_for :head do %>
  <%= javascript_include_tag "build/main", "data-turbolinks-track" => true %>
<% end %>

<% @posts.each do |post, n| %>
  <div>
    <h2><%= post.user.name %><h2>
    <h3><%= post.title %></h3>
    <p><%= post.text %></p>
  </div>
<% end %>
